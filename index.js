//document.getElementById('count-el').innerText = 5; 

// somewhere to store the data we need variables to store the data

// let count be zero
// Somewhere to store the data: variables to store the data

// Let count be zero
let count = 0;

console.log(count);
